<view class="message-item" bindtap="onTap">
  <view class="row-between">
    <view class="title">{{title}}</view>
    <status-badge text="{{levelText}}" type="{{type}}" size="small" />
  </view>
  <view class="time">{{time}}</view>
</view>
