# 交付检查清单

## 1. 代码与配置

- [ ] `.env` 只在本地保存，不提交仓库。
- [ ] `.env.example` 中没有真实生产密钥。
- [ ] `miniprogram/config/env.ts` 的 `baseURL` 指向当前 Gateway。
- [ ] `JWT_SECRET` 在非演示环境中已替换为强密钥。
- [ ] MySQL、Redis、Nacos 地址与当前环境一致。

## 2. 后端启动

- [ ] 已启动 MySQL、Redis、Nacos。
- [ ] `backend/scripts/check-infra.ps1` 检查通过。
- [ ] Auth、Operation、Analysis、Gateway 四个服务均已启动。
- [ ] `backend/scripts/check-services-health.ps1` 检查通过。

## 3. 自动化验证

```powershell
cd backend
mvn test
```

验收要求：

- [ ] 全后端测试 `BUILD SUCCESS`。
- [ ] Gateway 测试覆盖鉴权与 `X-Trace-Id`。
- [ ] Operation 测试覆盖申请、审批、补材料、操作日志。
- [ ] Analysis 测试覆盖缓存命中、缓存未命中、降级返回。

### V1.2 Web Admin 验收记录

- [x] `web-admin` 执行 `npm run build` 成功。
- [x] 全后端 68 项测试通过，0 失败。
- [x] 审批详情展示申请人、资源、节点、记录和操作日志。
- [x] 已处理审批保持只读，审批完成后列表和通知数量刷新。
- [x] 资源档案支持独立路由、关联审批、运营时间线和现场照片预览。
- [x] 资源地图展示真实经纬度点位，并可下钻到资源档案和审批详情。
- [x] 通知、搜索、用户菜单、KPI、资源类型和智能建议均有明确结果。
- [x] CSV 包含 UTF-8 BOM，中文字段和日期文件名正常。
- [x] 浏览器主流程回归完成，控制台无错误。
- [x] 产品化节点：`v1.2-web-admin-productized`，对应提交 `74c8c85`。

不属于 V1.2 节点的后续能力：完整 GIS、生产对象存储与病毒扫描、持久化通知中心和自动化 E2E。

### V1.3 Admin Pro 第六批验收记录

- [x] 系统设置由后端持久化，ADMIN 可修改，STAFF 只读，USER 返回 403。
- [x] 平台名称、村域名称和版本信息可同步到管理端布局。
- [x] 默认地图中心可通过资源地图“回到村域中心”真实使用。
- [x] 个人中心支持资料维护、密码修改和近期登录记录，密码修改后旧会话失效。
- [x] 通知由真实待审批和超时流程生成，已读状态按账号持久化并支持全部已读。
- [x] 顶栏通知与完整通知中心数量一致，通知可跳转对应审批上下文。
- [x] 系统设置和账号安全操作进入统一审计链路。
- [x] Web Admin 生产构建通过，后端全量 117 项测试通过，0 失败、0 错误。

V1.3 正式版剩余发布门：最小管理员 E2E、演示数据复位、全量验收记录和版本节点发布。

## 4. 演示账号

| 账号 | 密码 | 角色 | 用途 |
| --- | --- | --- | --- |
| `user_demo` | `123456` | USER | 小程序用户提交合作申请 |
| `staff_demo` | `123456` | STAFF | 工作人员处理审批和补材料 |
| `admin` | `123456` | ADMIN | 管理员刷新看板缓存和管理功能 |

## 5. 演示主流程

- [ ] 用户登录小程序。
- [ ] 浏览资源详情。
- [ ] 提交合作申请。
- [ ] STAFF/ADMIN 在协同工作台看到待处理申请。
- [ ] 执行通过、驳回或要求补材料。
- [ ] 用户在流程详情页看到审批记录和操作日志。
- [ ] Analysis 看板能展示业务指标。

## 6. 排障信息

出现接口问题时至少记录：

- 请求时间。
- 当前账号。
- 请求接口路径。
- HTTP 状态码。
- 响应头 `X-Trace-Id`。
- 后端服务控制台错误日志。

## 7. 发布收口

- [x] 当前功能分支已推送远端。
- [x] `v1.2-web-admin-productized` 已推送远端。
- [x] 阶段文档与代码节点一致。
- [x] 登录页、小程序和临时资源已分类处理，不混入 Web Admin 产品化提交。
- [ ] 合并主线后重新执行 Web 构建与后端全量测试。
- [ ] 主线工作区 `git status` 为 clean。
